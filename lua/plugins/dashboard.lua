return {
  "folke/snacks.nvim",

  opts = function(_, opts)
    -- opts.config.header = {

    -- }
    opts.dashboard.preset.header = [[
        k
     a
       v
|\.-*-.
|( ( 0 )
|/`*-*`

]]
  end,
}
